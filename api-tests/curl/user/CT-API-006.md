# CT-API-006 - Excluir usuário (DELETE)

```bash
curl --location --request DELETE 'https://serverest.dev/usuarios/{_id}'
```

**Esperado:**

```json
{
    "message": "Registro excluído com sucesso"
}
```
